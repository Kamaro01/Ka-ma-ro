import React from 'react';
import ReferralProgramInteractive from './components/ReferralProgramInteractive';

export const metadata = {
  title: 'Referral Program | Ka-ma-ro',
  description: 'Share Ka-ma-ro with friends and earn rewards through our referral program',
};

export default function ReferralProgramPage() {
  return <ReferralProgramInteractive />;
}
